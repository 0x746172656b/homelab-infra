apiVersion: velero.io/v1
kind: Schedule
metadata:
  name: authentik-daily
  namespace: velero
spec:
  schedule: "0 3 * * *"
  useOwnerReferencesInBackup: false
  template:
    includedNamespaces:
      - authentik
    snapshotMoveData: true
    ttl: 336h
