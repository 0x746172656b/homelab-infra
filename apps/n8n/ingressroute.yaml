apiVersion: traefik.io/v1alpha1
kind: IngressRoute
metadata:
  name: n8n-ingress-route
  namespace: n8n
spec:
  entryPoints:
    - websecure
  routes:
    - kind: Rule
      match: Host(`n8n.local.khider.fr`)
      services:
        - name: n8n
          port: 5678
  tls:
    secretName: n8n-cert
