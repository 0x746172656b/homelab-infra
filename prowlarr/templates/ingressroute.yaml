apiVersion: traefik.io/v1alpha1
kind: IngressRoute
metadata:
  name: prowlarr-ingress-route
  namespace: media
spec:
  entryPoints:
    - web
    - websecure
  routes:
    - kind: Rule
      match: Host(`prowlarr.khider.fr`)
      services:
        - name: prowlarr
          port: 8989
    - kind: Rule
      match: Host(`prowlarr.local.khider.fr`)
      services:
        - name: prowlarr
          port: 8989
  tls:
    secretName: prowlarr-cert
